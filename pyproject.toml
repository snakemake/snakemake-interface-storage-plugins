[tool.poetry]
name = "snakemake-interface-storage-plugins"
version = "1.1.2"
description = "This package provides a stable interface for interactions between Snakemake and its storage plugins."
authors = ["Johannes Koester <johannes.koester@uni-due.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
snakemake-interface-common = "^1.11.0"
wrapt = "^1.15.0"
reretry = "^0.11.8"
throttler = "^1.2.2"

[tool.poetry.dev-dependencies]
black = "^23.9.1"
coverage = {extras = ["toml"], version = "^7.3.2"}
flake8 = "^6.1.0"
flake8-bugbear = "^23.3.12"
pytest = "^7.0"
snakemake-storage-plugin-http = "^0.1.0"
snakemake = {git="https://github.com/snakemake/snakemake.git"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
